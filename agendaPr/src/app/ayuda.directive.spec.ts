import { AyudaDirective } from './ayuda.directive';

describe('AyudaDirective', () => {
  it('should create an instance', () => {
    const directive = new AyudaDirective();
    expect(directive).toBeTruthy();
  });
});
